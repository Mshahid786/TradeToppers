@model IEnumerable<TechBiteERP.Model.Models.City>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Pages/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<div class="row">
    <div class="col-lg-12">
        <div class="ibox float-e-margins">
            <div class="ibox-title">
                <h5>List of City</h5>

                <div class="ibox-content">
                    <table class="table dataTable_Responsive">
                        <thead>
                            <tr>
                                <th>
                                    @Html.DisplayNameFor(model => model.CreationDate)
                                </th>
                                <th>
                                    City
                                </th>
                                <th>
                                    @Html.DisplayNameFor(model => model.CompanyId)
                                </th>
                                <th>
                                    @Html.DisplayNameFor(model => model.BranchId)
                                </th>
                                <th></th>
                            </tr>
                        </thead>
                        <tbody>
                            @foreach (var item in Model)
                            {
                                <tr>
                                    <td>
                                        @Html.DisplayFor(modelItem => item.CreationDate)
                                    </td>
                                    <td>
                                        @Html.DisplayFor(modelItem => item.CityName)
                                    </td>
                                    <td>
                                        @Html.DisplayFor(modelItem => item.CompanyId)
                                    </td>
                                    <td>
                                        @Html.DisplayFor(modelItem => item.BranchId)
                                    </td>
                                    <td>
                                        <a asp-action="Create" asp-route-id="@item.CityID">Edit</a> |
                                        <a asp-action="Details" asp-route-id="@item.CityID">Details</a> |
                                        <a asp-action="Delete" asp-route-id="@item.CityID">Delete</a>
                                    </td>
                                </tr>
                            }
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
    </div>
</div>
