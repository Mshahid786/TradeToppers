@model IEnumerable<TechBiteERP.Model.Models.PlotFile>

@{
    ViewData["Title"] = "Plot File";
    Layout = "~/Pages/Shared/_Layout.cshtml";
}

<h2>Plot File</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<div class="row">
    <div class="col-lg-12">
        <div class="ibox float-e-margins">
            <div class="ibox-title">
                <h5>List of Plot File</h5>

                <div class="ibox-content">
                    <table class="table dataTable_Responsive">
                        <thead>
                            <tr>
                                <th>
                                    SR.No
                                </th>
                                <th>
                                    Plot
                                </th>
                                <th>
                                    Courier Name
                                </th>
                                <th>
                                    Tracking No
                                </th>
                                <th>
                                    Logged By
                                </th>
                                <th>
                                   
                                </th>
                            </tr>
                        </thead>
                        <tbody>
                            @foreach (var item in Model)
                            {
                                <tr>
                                    <td>
                                        @Html.DisplayFor(modelItem => item.Id)
                                    </td>
                                    <td>
                                        @Html.DisplayFor(modelItem => item.PlotId)
                                    </td>
                                    <td>
                                        @Html.DisplayFor(modelItem => item.CourierName)
                                    </td>

                                    <td>
                                        @Html.DisplayFor(modelItem => item.TrackingNo)
                                    </td>
                                    <td>
                                        @Html.DisplayFor(modelItem => item.LoggedBy)
                                    </td>
                                    <td>
                                        <a asp-action="Create" class="btn btn-sm btn-primary m-t-n-xs" asp-route-id="@item.Id"><i class="fa fa-angle-right"></i></a>
                                        <a asp-action="Delete" asp-route-id="@item.Id" class="btn btn-sm btn-danger m-t-n-xs"><i class="fa fa-trash"></i></a>
                                        <a href="~/Report/PrintBaseReport?id=@item.Id&&reportName=PlotFile">Print</a>
                                    </td>
                                </tr>
                            }
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
    </div>
</div>
